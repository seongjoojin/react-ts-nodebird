import React from 'react';

interface IProps {
  image: Array<{ src: string }>
}

const PostImage = ({image}: IProps) => {
  return (
    <div>
      구현중...
    </div>
  );
};

export default PostImage;